{
  "name": "tourist",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Tourist: code attribution tracking with local persistence and optional git-notes sharing.",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test": "vitest run",
    "test:watch": "vitest",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "devDependencies": {
    "@types/node": "^20.14.0",
    "typescript": "^5.5.4",
    "vitest": "^2.0.5"
  }
}
